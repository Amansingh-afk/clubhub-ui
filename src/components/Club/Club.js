import React from "react";
import Layout from "../Layout/Layout";
import ClubList from "./ClubList";

const Club = () => {
  return (
    <Layout>
      <ClubList />
    </Layout>
  );
};

export default Club;
